interface:
  display_name: "Skill Sync"
  short_description: "Sync Claude and Codex skills"
  default_prompt: "Use $skill-sync to compare and synchronize skill <skill-name> between Claude Code and Codex."

policy:
  allow_implicit_invocation: true
